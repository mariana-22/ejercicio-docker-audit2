# CVEs que se ignoran en el escaneo de Trivy (formato: un ID por línea).
# Última verificación con versions actualizadas (Flask 3.1.3, PyMySQL 1.2.0,
# cryptography 50.0.1): sin hallazgos críticos/altos en dependencias Python.
# Si el workflow falla, copia aquí el ID del CVE que quieras ignorar.